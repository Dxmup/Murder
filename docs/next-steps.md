# Next Design Steps

Completed in the V2.1 rebalance: the six dated R corpus excerpts (split objects O12a–c), the three escalating Vale incidents (historical inbox mail to Samira, Morgan, and Dee), distribution so no single recipient holds the Progressive reconstruction, regenerated fact and message audits, full C17–C20 goals and bargains, the four read-aloud ending scripts (`design/ending-reveals.md`), the Ending 4 threshold rule (`design/host-guide.md`), pre-game inbox rows in `design/data/messages.csv`, and the Kit disproof wiring (Rafi's ledger scene, Arden's print proof sheet).

1. Run the next 16-agent baseline with the canonical runner and compare against v2-baseline-14: watch for a weaker Human Mantle plurality, a real One Human constituency, more than 3 killed_by_ai votes, and at least three distinct Vale-relationship readings.
2. If the attractor persists, tune fact timing (offsets in `messages.csv`) before adding content; the affirmative material now exists in every section.
3. Produce the one-page per-role cheat cards referenced in the host guide.
4. Author the app's booklet-rendering pass so private-briefing emails are generated from `characters/` rather than duplicated into `messages.csv`.
5. Live playtest. The simulator cannot measure fun, charisma cascades, or accidental overhearing; the design questions that remain are human ones.
